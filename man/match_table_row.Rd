% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_highlighting.R
\name{match_table_row}
\alias{match_table_row}
\title{Match pipe-delimited table-row evidence to a single table row (internal)}
\usage{
match_table_row(evidence, table, min_fraction = 0.75)
}
\arguments{
\item{evidence}{Evidence string containing \code{|}}

\item{table}{Output of \code{parse_table_rows()}}

\item{min_fraction}{Minimum fraction of evidence cells that must match}
}
\value{
List with \code{row} (index of the \code{<tr>}) and \code{cells}
(indices of matched cells in that row), or \code{NULL} if no row matches
}
\description{
Splits the evidence on \code{|} and finds the \code{<tr>} containing the
most evidence cells in order, preferring exact cell matches. Cells of 4 or
more characters may differ slightly (OCR noise); shorter cells must match
exactly, so short values such as units or country codes are anchored by
the rest of the row rather than matched on their own.
}
\keyword{internal}
