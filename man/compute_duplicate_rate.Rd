% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{compute_duplicate_rate}
\alias{compute_duplicate_rate}
\title{Compute duplicate rate across benchmark trials}
\usage{
compute_duplicate_rate(
  benchmark_dir,
  unique_fields = c("Host_Name_std", "Pathogen_Name_std")
)
}
\arguments{
\item{benchmark_dir}{Directory containing \code{benchmark_records.csv} (with
standardized name columns from \code{\link[=standardize_benchmark_names]{standardize_benchmark_names()}})}

\item{unique_fields}{Character vector of column names that define record
uniqueness. Defaults to \code{c("Host_Name_std", "Pathogen_Name_std")},
matching the \code{x-unique-fields} schema for host-pathogen pair data.}
}
\value{
A list with three components:
\describe{
\item{per_paper}{Tibble with duplicate rate per paper-trial}
\item{per_trial}{Tibble with duplicate rate per trial (aggregate across papers)}
\item{aggregate}{List with overall duplicate rate, total records, and total duplicates}
}
}
\description{
Using harmonized names, counts within-paper-trial records sharing the same
unique field values. Measures how often the model produces duplicate records
that should have been merged.
}
\examples{
\dontrun{
dup_rates <- compute_duplicate_rate("data/test_variability")
dup_rates$aggregate
dup_rates$per_paper
}
}
