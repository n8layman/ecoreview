% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensorlake_render.R
\name{embed_page_images}
\alias{embed_page_images}
\title{Embed one page's stored images in its markdown (internal)}
\usage{
embed_page_images(page_md, images)
}
\arguments{
\item{page_md}{Markdown for one page}

\item{images}{The page's \code{images} list from \code{ocr_images}, each
with \code{id} and \code{image_base64}}
}
\value{
Markdown with placeholders replaced
}
\description{
Replaces each placeholder whose link target is a stored image's \code{id}
with an \code{<img>} tag holding that image's data. Images are matched by
\code{id}, not by position: Mistral numbers image ids across the whole
document, so a page's first image is not necessarily \code{img-0}.
}
\keyword{internal}
