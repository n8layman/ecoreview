% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_combine.R
\name{combine_db}
\alias{combine_db}
\title{Combine part databases back into a single database}
\usage{
combine_db(
  db_paths,
  output_path,
  overwrite = FALSE,
  pattern = "_part_\\\\d+\\\\.db$"
)
}
\arguments{
\item{db_paths}{A character vector of >= 2 part database file paths,
\strong{or} a single directory path. When a directory is supplied,
\code{pattern} is used to discover the part files within it.}

\item{output_path}{Path for the combined output database. Must not
already exist unless \code{overwrite = TRUE}.}

\item{overwrite}{If \code{TRUE}, delete \code{output_path} before combining.
Default \code{FALSE}.}

\item{pattern}{Regular expression used to filter files when \code{db_paths}
is a directory. Defaults to \code{"_part_\\\\d+\\\\.db$"}, which matches the
naming convention produced by \code{\link[=split_db]{split_db()}}.}
}
\value{
Path to the combined database (invisibly).
}
\description{
Merges multiple part databases (created by \code{\link[=split_db]{split_db()}}) into one,
verifying that all parts share an identical schema before proceeding.
}
\details{
\code{db_paths} can be supplied in two ways:
\itemize{
\item A character \strong{vector} of file paths (existing behaviour).
\item A \strong{single directory path} — all files in that directory whose names
match \code{pattern} are collected automatically and sorted before merging.
}

\strong{Schema check}: table names, column names, and column types must match
across all parts. Mismatched tables or columns raise an error;
mismatched column types raise a warning (SQLite's flexible typing often
makes this harmless, but you should investigate).

\strong{Overlap check}: document IDs are expected to be disjoint across parts.
Any overlap is reported as a warning; overlapping rows are still inserted
once (the duplicate is skipped with \verb{INSERT OR IGNORE}).

\strong{Primary-key remapping}: tables that carry their own auto-increment
integer primary key \emph{and} reference \code{interaction_id}
(e.g. \code{human_edits}) are inserted without their original PK so SQLite
assigns new non-conflicting values. All foreign-key relationships are
preserved because interaction IDs themselves are unique across parts.
}
\examples{
\dontrun{
# Supply a directory — part files discovered automatically
combine_db("review_parts/", output_path = "extractions_reviewed.db")

# Supply paths explicitly
parts <- list.files("review_parts", pattern = "_part_\\\\d+\\\\.db$",
                    full.names = TRUE)
combine_db(parts, output_path = "extractions_reviewed.db")
}
}
