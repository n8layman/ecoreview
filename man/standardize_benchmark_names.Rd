% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{standardize_benchmark_names}
\alias{standardize_benchmark_names}
\title{Standardize names across benchmark replicates}
\usage{
standardize_benchmark_names(
  benchmark_dir,
  name_columns = list(list(column = "Host_Name", type = "host", std_name =
    "Host_Name_std"), list(column = "Pathogen_Name", type = "pathogen", std_name =
    "Pathogen_Name_std")),
  model = NULL
)
}
\arguments{
\item{benchmark_dir}{Directory containing \code{benchmark_records.csv}}

\item{name_columns}{A list of lists, each with:
\describe{
\item{column}{Column name in the CSV to standardize}
\item{type}{Name type for the LLM prompt
(\code{"host"} or \code{"pathogen"})}
\item{std_name}{Name for the new standardized column}
}}

\item{model}{LLM model to use for standardization
(default: Claude via ellmer)}
}
\value{
The updated records tibble
(also overwrites \code{benchmark_records.csv})
}
\description{
Reads \code{benchmark_records.csv}, pools all unique names from specified
columns, sends each to an LLM for standardization, and adds
standardized columns to the CSV. Name mappings are cached to
\verb{name_mappings_\{label\}.csv} to avoid re-running the LLM on
subsequent calls.
}
\examples{
\dontrun{
# Default host-pathogen schema
records <- standardize_benchmark_names("data/test_variability")

# Custom schema
records <- standardize_benchmark_names(
  "data/test_variability",
  name_columns = list(
    list(column = "bat_species", type = "host",
         std_name = "bat_species_std"),
    list(column = "organism", type = "pathogen",
         std_name = "organism_std")
  )
)
}
}
